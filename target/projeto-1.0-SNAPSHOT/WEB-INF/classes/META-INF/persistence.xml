<persistence xmlns="https://jakarta.ee/xml/ns/persistence" version="3.1">
    <persistence-unit name="meuPU" transaction-type="RESOURCE_LOCAL">
        
        <!-- Entidades gerenciadas -->
        <class>com.exemplo.Pessoa</class>
        <class>com.exemplo.Endereco</class>

        <properties>
            <!-- Conexão com PostgreSQL -->
            <property name="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:postgresql://localhost:8081/web2database"/>
            <property name="jakarta.persistence.jdbc.user" value="usuario"/>
            <property name="jakarta.persistence.jdbc.password" value="admin"/>

            <!-- Dialeto do Hibernate para PostgreSQL -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>

            <!-- Configuração de criação/atualização de tabelas -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>

            <!-- Log SQL -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- Configurações opcionais de performance -->
            <property name="hibernate.jdbc.lob.non_contextual_creation" value="true"/>
        </properties>
    </persistence-unit>
</persistence>
